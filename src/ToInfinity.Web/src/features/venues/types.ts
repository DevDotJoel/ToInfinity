export interface Venue {
  id: string;
  userId: string;
  name: string;
  description: string;
  street: string;
  city: string;
  capacity: number;
  minPrice: number;
  maxPrice: number;
  mainImageUrl: string;
}
